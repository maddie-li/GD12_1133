using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class CombatRoom : RoomBase
{
    /*public CombatRoom(Vector2 coords, bool[] roomExits) : base(coords, roomExits)
    {
        this.Coords = coords;
        this.RoomExits = roomExits;

        SetExits(roomExits);
        SetRoomLocation(coords);
    }*/
}
